# Тесты identity

```bash
cd GastroRoute_identity
set PYTHONPATH=src
python -m unittest discover -s tests -p "test*.py" -v
```

- `test_user_list_schema.py` — схема `UserListResponse` (админ-список пользователей, план P2)
